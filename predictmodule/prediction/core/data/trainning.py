CONF = {
    'batch_size': 1000,

}

def get_last

def get_available_chunk(self, instance_meta, fetch_class):
    params = {
        'endpoint': endpoint,
        'db_name': db_name,
        'batch_size': CONF['batch_size']
    }

    fetch = fetch_class(**params)
